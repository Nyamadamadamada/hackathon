type Props = {
  name: string;
  comment: string;
  filename: string;
  tags: string[];
};
const UserListItem = (props: Props) => {
  // 演習1-1-2 UserListにあるここから〜ここまでのReact要素を移す
  return <></>;
};

export default UserListItem;
